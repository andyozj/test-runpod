# Samples

Generated by endpoint `7jrg4nu4b47fsv` (image `0.1.0-72e537d-slim`, L40S,
`black-forest-labs/FLUX.1-dev@0ef5fff`), 2026-08-06. Reproduce any of them:

```bash
python client/generate.py "<prompt>" --seed <seed>
```

| File | Prompt | Seed | Inference |
|---|---|---|---|
| `fox-seed42.png` | a red fox in falling snow, cinematic lighting | 42 | 14.9s* |
| `lighthouse-seed7.png` | a lighthouse on a cliff at dusk, dramatic clouds, golden hour | 7 | 22.3s |
| `owl-seed1234.png` | a clockwork owl made of brass gears, macro photography | 1234 | 21.3s |

*Generated on the first deploy (`0.1.0-44c9643-slim`); worker landed on an
A100 fallback before the GPU list was narrowed, hence the faster step time.

Same seed + same GPU type reproduces the image. Across GPU types, bf16 kernel
selection differs and pixels drift microscopically — the seed and dimensions
are the contract, not the bits.
