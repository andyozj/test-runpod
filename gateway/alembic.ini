# Alembic configuration. `sqlalchemy.url` is never set here: the runner
# (gateway.adapters.schema.upgrade_to_head) injects DATABASE_URL at runtime,
# and `migrations/env.py` falls back to the DATABASE_URL environment variable
# for manual CLI use (`uv run alembic upgrade head`).

[alembic]
script_location = migrations
