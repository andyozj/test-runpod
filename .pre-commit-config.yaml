repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-json
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.7.4
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(worker|gateway|client|scripts|benchmarks)/
      - id: ruff-format
        files: ^(worker|gateway|client|scripts|benchmarks)/

  - repo: local
    hooks:
      - id: mypy-worker
        name: mypy (worker)
        entry: bash -c 'cd worker && uv run mypy src/'
        language: system
        pass_filenames: false
        files: ^worker/
      - id: mypy-gateway
        name: mypy (gateway)
        entry: bash -c 'cd gateway && uv run mypy src/'
        language: system
        pass_filenames: false
        files: ^gateway/
      - id: lint-imports
        name: import-linter (gateway)
        entry: bash -c 'cd gateway && uv run lint-imports'
        language: system
        pass_filenames: false
        files: ^gateway/
