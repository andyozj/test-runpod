# Applies to both root-context builds: worker/Dockerfile and gateway/Dockerfile
# (compose.yaml builds the gateway with context `.`). Blacklist, not whitelist,
# so neither build loses files it copies.
.git
.claude
docs
**/.venv
**/__pycache__
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache
**/.import_linter_cache
**/.coverage
.env
